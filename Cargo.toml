[package]
name = "cw-emf-mocker"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }
metrics = "0.24"
metrics_cloudwatch_embedded = "0.10"
rand = "0.8"
rand_distr = "0.4"
uuid = { version = "1", features = ["v7"] }
